// function sum(a, b) {
//     return a + b;
// }
// // sum(10, '20');  // 1020
// var result = sum(10, 20);
// result.toLocaleString();

// @ts-check    // JavaScript 파일에서 타입 체크를 수행

/**
 * 
 * @param {number} a 첫번째 숫자
 * @param {number} b 두번째 숫자
 */
function sum(a, b) {
    return a + b;
}
sum(10, '20');
